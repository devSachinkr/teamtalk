import { Workplaces } from "@/types/app";
import React from "react";

type Props = {
  userWorkPlaceData: Workplaces[];
};

const Sidebar = ({ userWorkPlaceData }: Props) => {
  return <div>Sidebar</div>;
};

export default Sidebar;
